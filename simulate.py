''' the model is about simulate the physics situation'''
class simulate (object):
''' the thita means the angle of zhe panalle line and the needle it between 0 to PI,the l means the length of the needle .the a means the middle point of the needle depart from the panalle line'''
    def __init__(self,thita,a):
        self.thita=thita
        self.l=l  '''the needle'''
        self.a=a
    def needle(n):
        simulate.l=rand.rand(n)
        simulate.thita=rand.rand(n+1)
class rand():
    def rand(n):
        